class Subject:
    def __init__(self, code, section, name, room, color):
        self.code = code
        self.section = section
        self.name = name
        self.room = room
        self.color = color
